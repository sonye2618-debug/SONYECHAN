{
  "entities": {
    "Visitor": {
      "title": "Visitor",
      "description": "Information about a visitor who entered the portfolio.",
      "type": "object",
      "properties": {
        "companyName": {
          "type": "string",
          "description": "The name of the company entered by the visitor."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "The time when the visitor entered."
        },
        "userAgent": {
          "type": "string",
          "description": "The visitor's browser user agent."
        }
      },
      "required": ["companyName", "timestamp"]
    },
    "Feedback": {
      "title": "Feedback",
      "description": "User feedback from the portfolio.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the person giving feedback."
        },
        "company": {
          "type": "string",
          "description": "The company of the person giving feedback."
        },
        "content": {
          "type": "string",
          "description": "The feedback message text."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The time when the feedback was submitted."
        }
      },
      "required": ["content", "createdAt"]
    }
  },
  "firestore": {
    "/visitors/{visitorId}": {
      "schema": "Visitor",
      "description": "A collection recording portfolio access by company name."
    },
    "/feedbacks/{feedbackId}": {
      "schema": "Feedback",
      "description": "Collection of feedback submitted by recruiters or visitors."
    }
  }
}
